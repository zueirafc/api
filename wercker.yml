ruby: 2.2.3
box: phusion/passenger-ruby22

services:
  - postgres

build:
    steps:
        - bundle-install
        - script:
            name: rspec
            code: bundle exec rspec

deploy:
    steps:
        - heroku-deploy:
            key-name: herokudeployment
